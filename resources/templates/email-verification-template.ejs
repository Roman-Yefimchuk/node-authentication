<div style="padding: 15px;
            background-color: #f5f5f5;
            border: 1px solid #e3e3e3;
            border-radius: 4px;
            -webkit-box-shadow: inset 0 1px 1px rgba(0, 0, 0, .05);
            box-shadow: inset 0 1px 1px rgba(0, 0, 0, .05);"
        >
    <h1>Node Authentication</h1>

    <p>
        Hello, <b><%= userName %></b>

    <p>
        You have received a request to verify your email. Please click the following link to verify your email:

    <p>
        <a href="<%= host %>/api/email-verification/<%= token %>">
            <%= host %>/api/email-verification/<%= token %>
        </a>

    <p>
        If you did not request an email verification, you can ignore this email or contact us at <%= supportEmail %>.
</div>